let a$ = "foo "
let b$ = "foo"
let c$ = a$ + " this " + b$
let d$ = a$ + b$

print c$
print d$
print b$ + a$

print a$, " <  ", b$, " = ", a$ < b$
print a$, " >  ", b$, " = ", a$ > b$
print a$, " <= ", b$, " = ", a$ <= b$
print a$, " >= ", b$, " = ", a$ >= b$
print a$, " =  ", b$, " = ", a$ = b$
print a$, " <> ", b$, " = ", a$ <> b$

print "ana" < "are mere"
